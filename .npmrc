# Ensure platform-specific optional deps (e.g. rolldown / esbuild native
# bindings) are always resolved in the lockfile, so `npm ci` works on every
# CI platform regardless of where the lockfile was regenerated.
include=optional
